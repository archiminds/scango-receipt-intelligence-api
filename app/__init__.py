"""Top-level package for the ScanGo Receipt Intelligence API.

Keeping this file present makes the ``app`` directory an explicit Python
package, which is important for Lambda's ``app.api.handler.lambda_handler``
import path and for local pytest/script imports.
"""
